name: DNS maintenance v1

on:
  workflow_dispatch:
    inputs:
      dry_run:
        description: "Run checks only; do not modify or commit managed Grok DNS state"
        required: false
        default: true
        type: boolean

  schedule:
    - cron: "11 3,9,15,21 * * *"
      timezone: "Europe/Helsinki"

permissions:
  contents: write

concurrency:
  group: dns-maintenance-grok-${{ github.ref }}
  cancel-in-progress: false

jobs:
  maintain:
    uses: DKHNV/DNS-Maintenance/.github/workflows/reusable-maintenance.yml@0ebf730deb89a3aa0a1dac31137103e3cd12b405
    with:
      config_path: dns-maintenance-v1.json
      collection: grok
      dry_run: ${{ github.event_name == 'workflow_dispatch' && inputs.dry_run }}
      engine_ref: 0ebf730deb89a3aa0a1dac31137103e3cd12b405
